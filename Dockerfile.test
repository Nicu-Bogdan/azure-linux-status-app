FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY app.py .
COPY tests/ tests/

CMD ["sh", "-c", "python -c \"from app import init_db; init_db()\" && python -m pytest -v"]
